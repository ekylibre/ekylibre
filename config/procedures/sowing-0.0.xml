<?xml version="1.0"?>
<procedures xmlns="http://www.ekylibre.org/XML/2013/procedures">
  <procedure name="sowing" natures="sowing" version="1" status="frozen">
    <variables>
      <variable name="seeds" variety="plant" abilities="germinate"/>
      <variable name="seeds_to_sow" same-variant-of="seeds" parameter="false" new="quantity=?" roles="sowing-input"/>
      <variable name="sower" abilities="sow" />
      <!-- paramètre réprésentant des objets qui possede la compétence "Semer"  -->
      <variable name="driver" variety="worker"/>
      <!-- paramètre réprésentant des objets de type Humain qui possede la compétence "Conduire un ensemble tracteur + épandeur"  -->
      <variable name="tractor" abilities="tow"/>
      <!-- paramètre réprésentant des objets qui possede la compétence "Tracter"  -->
      <variable name="land_parcel" variety="cultivable_land_parcel" roles="sowing-target"/>
      <variable name="culture" variety="plant" roles="sowing-culture"/>
    </variables>
    <operations>
      <operation duration="4 minutes">
        <task do="[driver] moves-in [tractor]"/><!-- le chauffeur se déplace vers le tracteur et rentre dedant (?) -->
      </operation>
      <operation duration="4 minutes">
        <task do="[tractor] moves-to [sower]"/><!-- le chauffeur et le tracteur se déplace vers le lieu actuel de la charrue-->
      </operation>
      <operation duration="7 minutes">
        <task do="[tractor] catches [sower]"/><!-- le chauffeur attache la charrue au tracteur -->
      </operation>
      <operation duration="4 minutes">
        <task do="[tractor] moves-to [seeds]"/><!-- le chauffeur, tracteur et le semoir se déplacent vers le lieu actuel des semences-->
      </operation>
      <operation duration="1 minutes">
        <task do="[seeds] parts-with [seeds_to_sow]"/><!--  on sépare les semences que l'on va mettre dans le semoir du stock --> 
      </operation>
      <operation duration="4 minutes">
        <task do="[seeds_to_sow] moves-in [sower]"/><!-- le chauffeur charge des semences dans le semoir qui est un lieu de stockage temporaire de semences (normalement plus complexe que cela)-->
      </operation>
      <operation duration="18 minutes">
        <task do="[tractor] moves-to [land_parcel]"/><!-- le chauffeur + son tracteur et son semoir chargé de semences se déplace vers la parcelle et rentre dedant (?) -->
      </operation>
      <operation>
        <!-- tant que le [tractor] travaille (? = sous-entendu le tracteur tourne,le conducteur roule, le semoir sème) les task tournent -->
        <task do="[seeds_to_sow] moves-in [land_parcel]"/>
        <!-- les semences tombe par terre car le semoir tourne  -->
        <task do="[land_parcel] consumes [seeds_to_sow]"/>
        <!-- la parcelle consomme les semences épandues -->
        <task do="[land_parcel] produce [culture](quantity::?, unit::[land_parcel].unit)"/>
        <!-- les semences tombées sur le sol produise une sole de blé -->
        <task do="[driver] measure [land_parcel] at in_production"/>
        <!-- le chauffeur du tracteur mesure / observe l'état de la parcelle au niveau de son passage en production (valeur attendue = true)-->
        <!-- a terme, on devrait être capable de faire IF le semoir est vide alors j'arrete de semer -->
        <!-- et je retourne charger des semences sur le tas global-->
      </operation>
    </operations>        
  </procedure>
</procedures>
