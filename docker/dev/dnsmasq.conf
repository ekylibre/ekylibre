# Wildcard DNS local pour le développement Ekylibre
# Résout *.ekylibre.lan → 127.0.0.2 (loopback hôte)
address=/.ekylibre.lan/127.0.0.2

# Fallback DNS pour le reste des requêtes
server=8.8.8.8
server=1.1.1.1

# Pas de cache (dev uniquement)
cache-size=0
