=form_tag({}) do
  %fieldset
    %legend=tc(:export)
    =formalize do |f|
      -f.field content_tag(:label, tc(:with_prints), :for=>:with_prints), check_box_tag(:with_prints)
    .actions=submit_tag tc(:backup), :name=>'backup'

=form_tag({}, :multipart=>true) do
  %fieldset
    %legend=tc(:import)
    =formalize do |f|
      -f.field tc(:upload), file_field(:file, :path)
    .actions=submit_tag tc(:restore), :name=>'restore'


