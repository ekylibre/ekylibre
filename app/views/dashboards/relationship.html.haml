=render :partial=>"entities/search"

.menu
  %h2= tg('indicator')
  %ul
    =li_link_to tc('x_entities',:count=>Entity.count), :controller=>:entities, :action=>:index

-if @current_user.future_events.count > 0
  %h2=tc(:future_entity_events)
  =list(:my_future_entity_events)

-if EntityEvent.count > 0
  %h2=tc(:recent_entity_events)
  =list(:recent_entity_events)

