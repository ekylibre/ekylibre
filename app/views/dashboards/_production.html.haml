-unvalidateds = @current_company.operations.find(:all, :conditions=>{:moved_on=>nil})
.menu.indicator
  %h2= tc('indicator')
  %ul
    %li= link_to tc('x_performed_operations', :count=>unvalidateds.size), :action=>:unvalidateds, :controller=>:operations
