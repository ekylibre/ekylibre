-unvalidateds = Operation.unvalidateds
-#.menu.indicator
  %h2= tc('indicator')
  %ul
    %li= link_to tc('x_performed_operations', :count=>unvalidateds.count), :action=>:unvalidateds, :controller=>:operations

=beehive(:production) do
  -cell :demo_bar
  -hbox do
    -cell :last_events
    -cell :demo_pie
  -hbox do
    -cell :placeholder
    -cell :placeholder
    -cell :example do
      %h1 Direct HTML without controller
      %span But quite easy to manage
  -tabbox do
    -cell :placeholder
    -cell :demo_pie
    -cell :demo_bar
