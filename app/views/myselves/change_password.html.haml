=form_tag(nil, {:class=>"flex vbox", :style=>'overflow:none'}) do
  .fields.flex
    =formalize do |f|
      -f.error 'user'
      -f.title tc("insert_actual_password")
      -f.field :user, :old_password, :required => true
      -f.title tc("insert_password")
      -f.field :user, :password, :required => true
      -f.field :user, :password_confirmation, :required => true
    .end
  .actions=submit_tag tg(:apply)
