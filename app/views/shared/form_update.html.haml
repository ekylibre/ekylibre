%h1=t "#{self.controller.controller_name.to_s}.#{action_name.to_s}.title", {:label=>@options[:label]}
- form_tag do
  =render :partial=>@partial
  .actions
    =submit_tag(t (@options[:submit]||:update).to_s)
    =link_to(t((@options[:cancel]||:cancel).to_s),:back)

