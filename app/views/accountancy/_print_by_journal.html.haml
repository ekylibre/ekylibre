=formalize do |f|
  -f.title lc(:dates)
  -f.field lc(:from), text_field(:printed, :from, :size=>10)
  -f.field lc(:to), text_field(:printed, :to, :size=>10)
  -f.field lc(:journal), select(:printed, :name, @current_company.journals.collect{|p| [p.name, p.id]})

       
