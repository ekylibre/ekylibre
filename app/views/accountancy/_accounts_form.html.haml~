-if @account
  =formalize(:inner_form=>true) do |f|
    - f.error :account
    - f.title :title
    - f.field :account, :name
    - f.field :account, :label
    - f.field :account, :number  
    - f.field :account, :parent_id  
-elsif @entries
  %table
    %tr
      %td.label=lc :entry_record
      %td.label=lc :entry_name
    - @entries.each do |entry|
      %tr
        %td=entry.record_id
        %td=entry.name
        
     
