=formalize do |f|
  - f.error :entry
  - f.field :entry, :name
  - f.field :entry, :account_id, :choices=>@current_company.accounts.find(:all,  :order=>:name).collect{|x| [x.number, x.id]}
  - f.field :entry, :currency_debit
  - f.field :entry, :currency_credit

