%h1=lc(:title)
%ul
  %li=lc :list_of_statements
    - form_tag(:action => 'operations') do
      %table
        %tr
          %td.label=lc :select
          %td.select=select "journal", "id",  @journals.collect{|j| [j.name, j.id]}
        
   
