=formalize do |f|
  -f.title lc(:dates)
  -f.field lc(:from),text_field(:printed, :from)
  -f.field lc(:to),text_field(:printed, :to)
  -f.field lc(:journal_name),select(:printed, :name, Journal.find(:all).collect {|p| [ p.name]})

       
