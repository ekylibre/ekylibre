=formalize do |f|
  - f.error :cash
  - f.title :rib
  - f.field :cash, :name
  - f.field :cash, :bank_name
  - f.field :cash, :address, :field=>:textarea
  - f.title BankAccount.human_name
  - f.field :cash, :mode, :field=>:radio, :choices=>BankAccount.modes  
=formalize(:id=>:bban) do |f|
  - f.field :cash, :bank_code, :size=>10
  - f.field :cash, :agency_code, :size=>10
  - f.field :cash, :number
  - f.field :cash, :key, :size=>4
=formalize(:id=>:iban) do |f|  
  - f.field :cash, :iban
  - f.field :cash, :bic
=formalize do |f|  
  - f.title :accountancy
  - f.field :cash, :account_id,  :choices=>{:reflection=>:banks_accounts}, :new=>{:action => :account_create}
  - f.field :cash, :currency_id, :choices=>{:reflection=>:currencies}
  - f.field :cash, :journal_id,  :choices=>{:reflection=>:journals}, :new => {:action => :journal_create}
 

:javascript
  window.banToggle = function() {
    toggleElement('iban', $('cash_mode_iban').checked, 'bban');
  }
  banToggle()

=observe_field "cash_mode_iban", :frequency => 0.25, :function =>"banToggle()"
