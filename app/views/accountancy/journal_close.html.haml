- form_tag do
  =formalize do |f|
    - f.error :journal
    - f.title :select_closure_date
    - f.field :journal, :closed_on, :choices => @journal.closures.collect{|d| [::I18n.l(d), d]}
  .actions
    =submit_tag(tc('close'))
    =link_to_back(:label=>:cancel)
