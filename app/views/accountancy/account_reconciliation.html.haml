.search
  =keishiki_tag({}, :method=>:get) do
    %table
      %tr
        %td.crit
          %label=tc("search")
          =text_field_tag :key, params[:key]||session[:account_key]
        %td.submit=submit_tag tg("search_go"), :name=>nil
=kame(:account_reconciliation)
