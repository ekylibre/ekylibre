=formalize do |f|
  - f.error :city
  - f.field :city, :district_id, :choices => @current_company.districts.find(:all).collect {|d| [d.name, d.id]}, :options=>{:include_blank=>true}, :new => {:action => :districts_create}
  - f.field :city, :name
