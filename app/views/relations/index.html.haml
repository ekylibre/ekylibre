=render :partial=>"shared/entities_search"

.menu.indicator
  %h2= tg('indicator')
  %ul
    =li_link_to tc('x_entities',:count=>@current_company.entities.count), :controller=>:relations, :action=>:entities

-if @current_user.future_events.size > 0
  %h2=tc(:future_events)
  =kame(:user_future_events)

-if @current_company.events.size > 0
  %h2=tc(:recent_events)
  =kame(:recent_events)

