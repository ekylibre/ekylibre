=render :partial=>"shared/entities_search"

.menu.indicator
  %h2= tg('indicator')
  %ul
    =li_link_to tc('nb_entities',:count=>@entities.size), :controller=>:relations, :action=>:entities

-if @current_user.future_events.size > 0
  %h2=tc(:future_events)
  =dyta(:user_future_events)

-if @current_company.events.size > 0
  %h2=tc(:recent_events)
  =dyta(:recent_events)

