=formalize do |f|
  - f.error :area
  - f.field :area, :district_id, :choices => @current_company.districts.find(:all, :order=>:name).collect {|d| [d.name, d.id]}, :options=>{:include_blank=>true}, :new => {:action => :districts_create}
  - f.field :area, :name
  - f.field :area, :code
  - f.field :area, :country, :choices=>countries, :options=>{:include_blank=>true}
