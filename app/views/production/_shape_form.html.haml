= formalize do |f|
  -f.title :infos
  -f.error :shape
  -f.field :shape, :name
  -f.field :shape, :number
  -#f.field :shape, :polygon
  -f.field :shape, :area_measure
  -f.field :shape, :area_unit_id, :choices=>{:reflection=>:surface_units}, :new=>{:controller=>:company, :action=>:unit_create, :base=>'m2'}
  -#f.field :shape, :master
= formalize(:id=>:master_select) do |f|
  -#f.field :shape, :parent_id, :choices=>{:reflection=>:shapes}
= formalize do |f|
  - f.field :shape, :description, :field=>:textarea

:javascript
  window.master = function() { toggleElement('master_select', $('shape_master').checked); };
  master();
=observe_field("shape_master", :frequency=>0.25, :function=> "master();")
