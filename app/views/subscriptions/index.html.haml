=kujaku do |k|
  -k.criterion do
    =select_tag(:nature_id, options_for_select(@current_company.subscription_natures.collect{|x| [x.name, x.id]}, :selected=>params[:nature_id].to_i), "data-dependents"=>"#subscriptions_options")
    %span#subscriptions_options{"data-refresh"=>url_for(), "data-refresh-mode"=>"update"}=render :partial=>"options"

=list
