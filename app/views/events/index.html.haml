-# Eclated automatically (2011-06-08)
=form_tag({}, :method=>:get) do
  .search
    %table
      %tr 
        %td.crit
          =tc(:search)
          =text_field_tag("key", params[:key], :size=>30)
        %td.submit=submit_tag tg(:search_go), :name=>nil

=toolbar do
  -link :new
=list
