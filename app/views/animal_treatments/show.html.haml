=toolbar do |t|
  -t.edit @animal_treatment
  -t.print_entity @animal_treatment
  -t.destroy(@animal_treatment, :method=>:delete, "data-confirm" => tg(:are_you_sure_you_want_to_delete)) if @animal_treatment.destroyable?

=attributes_list(@animal_treatment) do |l|
  %fieldset
    -l.attribute :name