=toolbar do |t|
  -t.edit @animal_diagnostic
  -t.print_entity @animal_diagnostic
  -t.destroy(@animal_diagnostic, :method=>:delete, "data-confirm" => tg(:are_you_sure_you_want_to_delete)) if @animal_diagnostic.destroyable?

=beehive do |b|
  -b.cell do
    =attributes_list(@animal_diagnostic) do |l| # liste des champs à afficher
      -l.attribute :name

  -b.tabbox do
