=toolbar do |t|
  -t.edit @diagnostic
  -t.print_entity @diagnostic
  -t.destroy(@diagnostic, :method=>:delete, "data-confirm" => tg(:are_you_sure_you_want_to_delete)) if @diagnostic.destroyable?

=attributes_list(@diagnostic) do |l| # liste des champs à afficher
  -l.attribute :name

=tabbox(:diagnostic) do |tb|