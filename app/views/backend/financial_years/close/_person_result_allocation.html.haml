- if @result + @carry_forward_balance >= 0
  = render 'backend/financial_years/close/positive_result_allocation_person'
- else
  = render 'backend/financial_years/close/negative_result_allocation_person'
