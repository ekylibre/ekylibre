- data = data_series(dimension, :"marketable_quantity", dataset)

.chart-cell= column_highcharts(data, title: {text: :stock_in_ground_by_calibration_x.tl(x: scale.name)}, x_axis: { categories: [''] }, y_axis: {reversed_stacks: false, title: {text: "#{symbol_measure}"}, stack_labels: {enabled: true}}, legend: true, tooltip: {point_format: "{point.y: 1f} #{symbol_measure}"}, plot_options: { column: {stacking: 'normal', data_labels: {enabled: true}}})
