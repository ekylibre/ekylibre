= main_toolbar do |t|
  - t.run  resource, method: :post
  - t.edit resource

= beehive do |b|
  - b.hbox do
    - b.cell do
      = attributes_list do |l|
        - l.attribute :name
        - l.attribute :active
        - l.attribute :nature
        - l.attribute :external
    - b.cell :guide_evolution, params: {id: resource.id}

  - b.cell :analyses do
    = list :analyses