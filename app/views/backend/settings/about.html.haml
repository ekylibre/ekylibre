#about-us
  = link_to("http://www.ekylibre.org", class: :logo) do
    = image_tag("logo.svg")
  - for p in @properties
    .med-info
      %span.title= p[0]
      %span.value= p[1]
  .med-info
    %span.title= "Source de données phytosanitaires"
    %p= "Données E-Phy - ANSES"
    %p= :updated_at.tl(date: @date)
