= f.referenced_association :assembly, as: :hidden
= f.referenced_association :schematic, as: :hidden

:ruby
  options = {
    new: false,
    source: {
      keep: true,
      scope: {
        components_of: f.object.schematic_id
      }
    },
    input_html: {
      class: "scoped-parameter",
      data: {
        intervention_updater: prefix + 'component_id'
      }
    }
  }

= f.referenced_association :component, options
