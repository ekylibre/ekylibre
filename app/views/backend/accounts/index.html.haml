= kujaku do |k|
  - k.text
  - k.crit :journal_period, :label => :accounts_only_used_at.tl, :default => "", :include_blank => :no_particular_periods

- main_toolbar do |t|
  - t.new
  - t.load_

= major_accounts_tabs_tag
= list
