= kujaku do |k|
  - k.text
  - k.choice [:all.tl, :all], *[:active, :inactive].map{ |s| [s.tl, s] }, name: :state
  - k.helper :journal_period_crit, :label => :accounts_only_used_at.tl, :default => "", :include_blank => :no_particular_periods

- main_toolbar do |t|
  = t.new params: { redirect: nil }, label: :add_an_account.tl

= major_accounts_tabs_tag

= main_list
