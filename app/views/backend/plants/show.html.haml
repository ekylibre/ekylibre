= main_toolbar do |t|
  - t.edit @plant
  - t.destroy(@plant, :method=>:delete, "data-confirm" => tg(:are_you_sure_you_want_to_delete))

= beehive do |b|
  - b.cell :details do
    .picture
      - if @plant.picture.exists?
        = image_tag(@plant.picture.url(:identity))
      - else
        = @plant.shape_svg(width: 180, height: 180, :class => :map)
    = attributes_list(@plant) do |l| # liste des champs à afficher
      - l.attribute :name
      - l.attribute :born_at
      - #-l.attribute :variety, url: true
      - #-l.attribute :group, url: true # on affiche l'attribut name de l'entité ProductGroup associé par l'attribut group de l'entité courante Product avec un lien vers ce groupe
      - l.attribute :description

  - b.tabbox do
    - b.cell(:intervention_casts) do
      = list(:intervention_casts)
    - b.cell(:incidents) do
      = toolbar do |t|
        - t.new(:params =>{:controller => :incidents, :target_id => @plant.id, :target_type => @plant.class.name})
      = list(:incidents)
    - b.cell(tl(:x_indicators, :count => @plant.indicator_data.count)) do
      = list(:indicators)
    - b.cell(:measurements) do
      = list(:measurements)
    - b.cell(:places) do
      = list(:places)

