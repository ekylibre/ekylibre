:ruby
  cursors = []
  data.inject(0){|index, item| cursors << index if ['stop'].include? item[:nature]; index += 1}
  cursors.pop

%div#slider

:coffee
  $ ->
    $("#slider").slider
      min: 0
      max: #{data.length - 1}
      values: #{cursors}
