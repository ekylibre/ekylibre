= infos do
  .big-info
    %span.title= :currency.t(scope: 'preferences')
    %span.value= Onoma::Currency[Preference[:currency]].human_name rescue "?"
  .big-info
    %span.title= :country.t(scope: 'preferences')
    %span.value= Onoma::Country[Preference[:country]].human_name rescue "?"
  .big-info
    %span.title= :language.t(scope: 'preferences')
    %span.value= Onoma::Language[Preference[:language]].human_name rescue "?"
  .big-info
    %span.title= :accounting_system.t(scope: 'preferences')
    %span.value= Preference[:accounting_system].mb_chars.upcase.gsub('_', '-') rescue "?"
  - fiscal_position = Onoma::FiscalPosition[Preference[:fiscal_position]]
  - if fiscal_position.country.to_s == Preference[:country]
    .big-info
      %span.title= :fiscal_position.t(scope: 'preferences')
      %span.value= Preference[:fiscal_position].mb_chars.upcase.gsub('_', '-') rescue "?"
