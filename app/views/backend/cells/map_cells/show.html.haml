- if @campaigns && @activity_productions.any?
  = visualization({async_url: backend_visualizations_map_cells_visualizations_path(@url_params)}, {}) do |v|
    - v.control :zoom
    - v.control :scale
    - v.control :fullscreen
    - v.control :layer_selector
    - v.center @activity_productions.first.support_shape_centroid
- else
  .placeholding-message= :no_production_defined_for_current_campaign.tn
