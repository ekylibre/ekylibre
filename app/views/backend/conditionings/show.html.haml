- main_toolbar do |t|
  = t.edit resource
  = t.destroy resource

= main_informations do
  = attributes_list do |l|
    - l.attribute :name
    - l.attribute :work_code
    - l.attribute :description
    - l.custom :correspondence.tl, pluralize(resource.format_coefficient, resource.base_unit.name)

= cobbles do |c|
  - c.cobble(:products) do
    = cobble_list(:products)
