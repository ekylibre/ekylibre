= kujaku do |k|
  - k.text
  - k.check_boxes :draft, :estimate, :aborted, :refused, :order, :invoice, name: :state
  - k.crit :journal_period
- main_toolbar do |t|
  - t.new variants: PurchaseNature.reorder(:by_default).to_a.inject({}){|h,n| h[n.name] = {nature_id: n.id}; h}
= list
