= field_set do
  = f.input :name, required: true
  = f.input :work_number
  = f.nested_association(:labellings , button_label: :add_use.tl, partial: '/backend/worker_groups/labelling_fields', collection: f.object.labellings.any? ?  f.object.labellings : f.object.labellings.new )
  %div.worker-group-item-fields
    = f.nested_association(:items, button_label: :add_worker.tl, partial: 'worker_field', collection: f.object.items.any? ?  f.object.items : f.object.items.new )
