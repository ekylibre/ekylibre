=field_set do
  =f.association :journal, :readonly => true
  =f.input :number
  =f.input :printed_on
  -partial :exchange_rate, :depend_on => [:printed_on, :journal], :refresh_url => new_journal_entry_url
