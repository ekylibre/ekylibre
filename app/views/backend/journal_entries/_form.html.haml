= field_set do
  = f.referenced_association :journal, :readonly => true
  = f.input :number
  = f.input :printed_at
  = render :partial => "exchange_rate_form", :depend_at => [:printed_at, :journal], :refresh_url => new_backend_journal_entry_path
