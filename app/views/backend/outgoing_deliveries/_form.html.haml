-# TODO Manage  extra content in forms
=sales_steps(@outgoing_delivery.sale)

=field_set do
  -# TODO: Manage filtered source with column
  =f.association :address, :new => {:entity_id => @outgoing_delivery.sale.client_id}
  =f.association :mode
  =f.input :planned_on
  =f.association :transporter, :source => :transporters, :new => {:transporter => 1}

=field_set :outgoing_delivery_lines do
  -partial :lines
