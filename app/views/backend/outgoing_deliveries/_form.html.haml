-# TODO Manage  extra content in forms
=sales_steps(@outgoing_delivery.sale)

=field_set do
  -# TODO: Manage filtered source with column
  -association :address, :new => {:entity_id => @outgoing_delivery.sale.client_id}
  -association :mode
  -input :planned_on
  -association :transporter, :source => :transporters, :new => {:transporter => 1}

=field_set :outgoing_delivery_lines do
  -partial :lines
