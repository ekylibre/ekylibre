=toolbar do |t|
  -t.edit @product_localization
  -t.print_entity @product_localization
  -t.destroy(@product_localization, :method=>:delete, "data-confirm" => tg(:are_you_sure_you_want_to_delete)) if @product_localization.destroyable?

=beehive do |b|
  -b.cell :details do
    =attributes_list(@product_localization) do |l| # liste des champs à afficher
      -l.attribute :container
