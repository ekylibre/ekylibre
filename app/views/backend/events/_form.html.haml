= field_set do
  = f.input :name
  -# = f.referenced_association :entity
  -# = f.referenced_association :responsible
  = f.referenced_association :nature
  = f.input :place, :autocomplete => true
  -# TODO Manages refresh_depending_at
  = f.input :started_at
  = f.input :stopped_at
  = f.input :duration, :refresh_depending_at => :nature
