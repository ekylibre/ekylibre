.integrations-container
  - @integration_types.each do |type|
    = link_to action: :new, controller: :integrations, nature: type.integration_name.underscore do
      .integration{class: ("integration-active" if Integration.find_by_nature(type.integration_name.underscore)) }
        .integration-logo
          = image_tag(integration_logo_path(type.integration_name))
        .integration-description
          %h3= type.integration_name
          %p= "#{type.integration_name.underscore}_short_description".tl
