-# TODO Check
= field_set do
  = f.referenced_association :sale_item, as: :hidden
  = f.referenced_association :nature, as: :hidden
  = f.input :quantity
  -# input :suspended
  = f.referenced_association :address
  = f.input :description
  = f.custom_fields

= field_set :interval do
  .date-subscription
    = f.input :started_at
    = f.input :stopped_at
  .number-subscription
    = f.input :first_number
    = f.input :last_number
