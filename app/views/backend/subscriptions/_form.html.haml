-# TODO Check
=field_set do
  =f.association :sale_item, :field => :hidden
  =f.association :nature
  =f.input :quantity
  -# input :suspended
  =f.association :address
  =f.input :description

=field_set :interval, :depend_on => :nature do
  =f.input :started_on, :if => :period?
  =f.input :stopped_on, :if => :period?
  =f.input :first_number, :if => :quantity?
  =f.input :last_number,  :if => :quantity?
