- main_toolbar do |t|
  - t.edit resource

= beehive do |b|
  - b.cell do
    = attributes_list do |l|
      - l.attribute :number
      - l.attribute :amount
      - l.attribute :payee, label: :full_name, url: true
      - l.attribute :responsible, label: :label
      - l.attribute :mode
      - l.attribute :paid_at
      - l.attribute :to_bank_at
      - l.attribute :bank_check_number
      - l.attribute :journal_entry, url: true
      - if resource.custom_fields.any?
        - l.custom_fields

  - b.cell :affair do
    = affair_of(resource)
