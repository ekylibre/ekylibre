=toolbar do |t|
  -t.edit @animal_disease
  -t.print_entity @animal_disease
  -t.destroy(@animal_disease, :method=>:delete, "data-confirm" => tg(:are_you_sure_you_want_to_delete)) if @animal_disease.destroyable?

=attributes_list(@animal_disease) do |l| # liste des champs à afficher
  -l.attribute :name
  -l.attribute :code
  -l.attribute :zone

=tabbox(:animal_disease) do |tb|
  -tb.tab(:cares) do
    =list(:cares)