=toolbar do |t|
  -t.edit @animal_race
  -t.print_entity @animal_race
  -t.destroy(@animal_race, :method=>:delete, "data-confirm" => tg(:are_you_sure_you_want_to_delete)) if @animal_race.destroyable?

=attributes_list(@animal_race) do |l| # liste des champs à afficher
  -l.attribute :parent
  -l.attribute :name
  -l.attribute :code
