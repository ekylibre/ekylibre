=toolbar do |t|
  -t.edit @prescription
  -t.print_entity @prescription
  -t.destroy(@prescription, :method=>:delete, "data-confirm" => tg(:are_you_sure_you_want_to_delete)) if @prescription.destroyable?

=attributes_list(@prescription) do |l|
  -l.attribute :name