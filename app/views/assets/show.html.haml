
=toolbar do |t|
  -t.edit(@asset)
  -t.cede(@asset)
  -t.depreciate(@asset, :method => :post)

=attributes_list(@asset) do |l|
  -l.attribute :name
  -l.attribute :depreciation_method
  -l.attribute :depreciable_amount, :currency => true
  -l.attribute :depreciated_amount, :currency => true
  -l.attribute :description
  -l.attribute :comment
  -l.attribute :started_on
  -l.attribute :stopped_on

=tabbox(:asset) do |tb|
  -tb.tab(:depreciations) do
    =list :depreciations
