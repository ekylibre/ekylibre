.count=tc(:nb_employees, :count=>@current_company.employees.size)

=render :partial=>'employees_search'

.aclist=link_to tc(:employees_create), {:action=>:employees_create}, :class=>'procedure create'
=dyta(:employees)
