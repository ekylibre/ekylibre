=toolbar do |t|
  -t.edit @animal_event
  -t.print_entity @animal_event
  -t.destroy(@animal_event, :method=>:delete, "data-confirm" => tg(:are_you_sure_you_want_to_delete)) if @animal_event.destroyable?

=attributes_list(@animal_event) do |l|
  %fieldset
    -l.attribute :name