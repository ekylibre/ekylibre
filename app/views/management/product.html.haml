=toolbar do
  -link :product_update, :id=>@product.id
  -link :products

%table
  %tr
    %td
      =evalue @product, :name
      =evalue @product, :price
      =evalue @product, :code
      =evalue @product, :code2
      =evalue @product, :ean13
    %td
      =evalue @product, :shelf, :url=>{:action=>:shelf}
      =evalue @product, :unit, :label=>:label
      =evalue @product, :weight
      =evalue @product, :active
    %td
      =evalue @product, :to if @product.to_sale or @product.to_purchase
      =#evalue @product, :catalog_name
      =#evalue @product, :catalog_description
      =evalue @product, :description
      =evalue @product, :comment
    %td
      =evalue @product, :manage_stocks

- if @product_stocks.size > 0
  %h2= tc('stocks')
  =dyta :uniq_critic_product_stocks

-if @product.to_sale or @product.to_purchase
  %h2=tc :prices
  =toolbar do |t|
    -t.link(:sale_price_create, {:action=>:price_create, :mode=>:sale_orders, :product_id=>session[:product_id]}) if @product.to_sale
    -t.link(:purchase_price_create, {:action=>:price_create, :mode=>:purchase_orders, :product_id=>session[:product_id]}) if @product.to_purchase

  =dyta :product_prices

%h2=tc :components
=toolbar do
  -link :product_component_create

=dyta :product_components
