-if @purchase_orders.size > 0
  =form_tag do
    =dyta :unreceived_purchases
    .actions
      =link_to tg(:previous), :action=>:index
      =submit_tag(tc('validate'), :action=>:unreceived_purchases)

-else
  %p{:style=>'text-align:center'}= tc('no_purchases')
 
