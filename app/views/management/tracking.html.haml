%table
  %tr
    %td
      =evalue @tracking, :name
      =evalue @tracking, :serial
    %td
      =evalue @tracking, :producer, :url=>{:action=>:entity, :controller=>:relations}
      =evalue @tracking, :product, :url=>{:action=>:product}

-if @tracking.stocks.size > 0
  %h2=tc(:stocks)
  =kame :tracking_stocks

-if @tracking.purchase_order_lines.size > 0
  %h2=tc(:purchases)
  =kame :tracking_purchase_order_lines

-if @tracking.operation_lines.size > 0
  %h2=tc(:operations)
  =kame :tracking_operation_lines

-if @tracking.sale_order_lines.size > 0
  %h2=tc(:sale_orders)
  =kame :tracking_sale_order_lines
