-form_tag() do
  =formalize do |f|
    - f.title :inventory
    - f.field :inventory, :responsible_id, :choices=>{:reflection=>:users}
    - f.field :inventory, :comment, :field=>:textarea
  =dyta :inventory_lines_create
  .actions
    =link_to tg(:previous), :action=>:inventories
    =submit_tag(tc('validate'), :action=>:inventory_create)
 
