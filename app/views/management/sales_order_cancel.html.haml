-if !session[:errors].nil?
  -if !session[:errors].empty?
    .errorExplanation
      %h2=session[:errors].size.to_s+" erreur(s) ont empéché l'enregistrement"
      %p Il y a eu des problèmes avec les champs suivantes :
      %ul
        -for error in session[:errors]
          %li=error.to_s

=error_messages('sales_order', :object=>@credit) if @credit

=form_tag() do
  =kame :sales_order_creditable_lines
  .actions
    =submit_tag(tc('validate'))
 
