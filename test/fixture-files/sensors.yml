vendor1:
  model1:
    label: "model1 label"
    description: "model1 description"
    indicators:
      - temperature
      - hygrometry
      - atmospheric_pressure
    # controller: "Vendor1::GenericController"